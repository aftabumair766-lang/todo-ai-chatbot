# Todo AI Chatbot - Environment Configuration Example
# ⚠️ IMPORTANT: Copy this to .env and replace with your actual credentials!

# Database (Using Neon PostgreSQL for production)
# Get your connection string from: https://neon.tech
DATABASE_URL=postgresql+asyncpg://user:password@your-host.neon.tech/dbname?ssl=require

# OpenAI API (⚠️ REQUIRED - Get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-your-api-key-here

# ChatKit Workflow ID (Optional - only if using ChatKit)
# After creating workflow in OpenAI platform, add the ID here
CHATKIT_WORKFLOW_ID=asst_your_workflow_id_here

# Better Auth (For production authentication)
# Generate a secure random string (32+ characters)
BETTER_AUTH_SECRET=your-32-character-secret-key-here
BETTER_AUTH_ISSUER=https://your-domain.com

# Redis (Optional for production - for rate limiting)
# Use Redis Cloud or local Redis
REDIS_URL=redis://localhost:6379/0

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
RATE_LIMIT_PER_MINUTE=10

# ============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Get OpenAI API key: https://platform.openai.com/api-keys
# 3. Get Neon PostgreSQL: https://neon.tech (free tier available)
# 4. Replace all placeholder values above with your actual credentials
# 5. Never commit .env to git (already in .gitignore)
# ============================================================================
