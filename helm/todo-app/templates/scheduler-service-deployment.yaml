apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.schedulerService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "todo-app.schedulerService.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.schedulerService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.schedulerService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.schedulerService.name }}
        {{- include "todo-app.schedulerService.labels" . | nindent 8 }}
      annotations:
        {{- if .Values.schedulerService.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: {{ .Values.schedulerService.dapr.appId }}
        dapr.io/app-port: "{{ .Values.schedulerService.dapr.appPort }}"
        dapr.io/log-level: {{ .Values.schedulerService.dapr.logLevel }}
        dapr.io/sidecar-cpu-request: {{ .Values.dapr.sidecarCPURequest }}
        dapr.io/sidecar-memory-request: {{ .Values.dapr.sidecarMemoryRequest }}
        dapr.io/sidecar-cpu-limit: {{ .Values.dapr.sidecarCPULimit }}
        dapr.io/sidecar-memory-limit: {{ .Values.dapr.sidecarMemoryLimit }}
        {{- end }}
    spec:
      containers:
      - name: {{ .Values.schedulerService.name }}
        image: "{{ .Values.schedulerService.image.repository }}:{{ .Values.schedulerService.image.tag }}"
        imagePullPolicy: {{ .Values.schedulerService.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.schedulerService.service.port }}
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: {{ .Values.global.environment }}
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: todo-secrets
              key: DATABASE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          {{- toYaml .Values.schedulerService.resources | nindent 10 }}
